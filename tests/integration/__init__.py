"""Tests d'intégration — services externes (Mongo, Neo4j, ES, Anthropic).

Marqués `@pytest.mark.integration`. Skip par défaut en CI sans secrets ;
exécuter en local avec `pytest -m integration`.
"""
